# Artifacts

This folder preserves the expected artifact layout while omitting private trace payloads and environment-specific generated outputs.

Use it as a structural placeholder for grounding packages, retrieval traces, model requests, and model responses in a private environment.
